# تقرير إصلاح وقبول الهاتف — Arsenal ATL

**حالة التسليم:** مكتمل بعد تدقيق مرئي ووظيفي على واجهات الهاتف، مع الحفاظ الكامل على معمارية TypeScript/Vite متعددة الصفحات. لا توجد عودة إلى iframe أو `toolLoader` القديم.

## ما تغير

تم نقل البحث إلى الشريط العلوي المشترك. يدعم البحث أسماء الأدوات والوصف العربي، ويعرض نتائج فورية ثم ينقل إلى مسار الأداة الصحيح. أزيل صندوق البحث المكرر من الصفحة الرئيسية، وعُدلت مسافة الغلاف كي تبدأ العناوين والمحتوى بوضوح بعد الشريط العلوي في الرئيسية والإعدادات والأدوات.

عولجت توافقات الوضع الفاتح للبطاقات وحقول الإدخال في الأدوات المتأثرة، ومنها مزيل الضوضاء ومحرر الصور وتشفير النصوص. كما قُيدت أبعاد SVG داخل محرر الصور، لذلك لا تتمدد أيقونات شريط الأدوات أو الشريط السفلي بعد تحميل صورة فعلية.

أعيد بناء القائمة المنسدلة المشتركة لتظل مرتبطة بالحقل نفسه في الهاتف وسطح المكتب. كان التموضع الثابت ينفصل عن الحقل داخل أقسام الأدوات ذات الحركة CSS؛ تستخدم القائمة الآن تموضعاً مطلقاً فوق محتوى البطاقة مع طبقة صحيحة، من دون تمرير الصفحة الأم عند الفتح.

| مجال التدقيق | النتيجة |
|---|---|
| الشريط العلوي والبحث | يظهران دون تجاوز أفقي؛ اختبار «ضغط فيديو» وصل إلى ضاغط الفيديو بنجاح. |
| الرئيسية والإعدادات | العناوين تبدأ أسفل الشريط العلوي، والبطاقات متوازنة على الهاتف. |
| محرر الصور | اختُبر بعد تحميل صورة؛ شريط الأدوات وأيقونات التصدير والتراجع ضمن أحجامها. |
| مصفاة النصوص وQR | البطاقات والعناصر الأساسية ضمن العرض؛ لا أيقونات أو معاينات ضخمة. |
| الأرشفة والتشفير | ظهرت قائمة الضغط فوق الحقول المجاورة ومتصلة بالحقل، بلا تمرير تلقائي للصفحة. |
| القوائم المشتركة | اختُبرت في ضاغط الفيديو، محول الصوت، إنشاء PDF، مصفاة النصوص، والأرشفة. |

## قبول قياس الهاتف

جرى التقاط وفحص جميع المسارات الـ21 على قياس CSS مقداره **393×852**. كانت قيمة عرض التمرير مساوية لعرض الصفحة في كل المسارات؛ لذلك لا يوجد تجاوز أفقي مخفي. كما أعاد فحص HTTP النهائي الرمز **200** لكل صفحة مستقلة.

## فحوص الجودة

```text
pnpm check  ✓
pnpm build  ✓
21/21 route HTTP checks  ✓
```

## تشغيله من Termux على الهاتف

> فك الحزمة داخل مجلد المنزل `~`، لا داخل `Download`، لأن Android يمنع الروابط الرمزية التي يحتاجها pnpm في مساحة التنزيل.

```bash
cd ~
tar -xzf arsenal-ts-mobile-ui-final.tar.gz
cd ~/arsenal-ts
pnpm install --frozen-lockfile
pnpm dev --host 127.0.0.1
```

بعد ظهور الرابط المحلي، افتحه من متصفح الهاتف. إذا كان Termux يستخدم اسم مضيف آخر في السجل، افتح الرابط الذي يعرضه Vite كما هو.

## تحديث محفوظ قبل الإطلاق: ضاغط الفيديو

عولجت لقطات الهاتف الأحدث لضاغط الفيديو. أعيدت صفوف الإعدادات إلى ترتيب أفقي مدمج: التسمية إلى اليمين وحقل الاختيار إلى اليسار، بدلاً من تكديسهما عمودياً. كذلك عولج تعارض نمط قديم كان يضيف حوافاً وحشوة إلى حاوية القائمة المخصصة؛ أصبحت بطاقة الإعدادات بارتفاع **229px** على عرض 393px، وكل صف بارتفاع **44px**، من دون تجاوز أفقي.

اختبرت قائمة الدقة مفتوحة على القياس نفسه. بقيت متصلة بالحقل، ضمن عرض الصفحة، ولم تمرر الصفحة عند الفتح. نجح أيضاً فحص TypeScript وبناء الإنتاج بعد هذا التحديث.

## تحديث محفوظ قبل الإطلاق: فيديو إلى صوت

عولج تدفق النتيجة بعد تحويل الفيديو على الهاتف. عادت إحصاءتا حجم الفيديو وحجم الصوت المستخرج إلى بطاقتين متجاورتين، ثم زر المعاينة وخط الزمن وصيغة التصدير وزر التنزيل، بدلاً من البطاقات العمودية الكبيرة. صارت قائمة الصيغة تعرض `WAV` و`MP3` باختصار واضح، مع وصف «خام ونقي 100٪» و«مضغوط وعالي التوافق» داخل الخيارات كما في الواجهة المرجعية.

اختبرت الأداة بملف MP4 محلياً: اكتمل استخراج الصوت، ظهرت الإحصاءات والمعاينة والتنزيل، وغيّر اختيار MP3 تسمية زر التنزيل إلى MP3. في اختبار 393×852 بقيت القائمتان ضمن العرض ومتصلة بالحقل من دون تمرير الصفحة.

## تحديث محفوظ قبل الإطلاق: مزيل الضوضاء

أزيلت رسالة النجاح النهائية المتكررة التي كانت تظهر أسفل زر «ملف آخر» بعد اكتمال إزالة الضوضاء. أصبحت عناصر المعاينة والتنزيل نفسها دليلاً كافياً على الجاهزية. في المقابل، تبقى رسالة جاهزية الملف أثناء اختيار ملف جديد ورسائل التحميل والخطأ مرئية عند الحاجة، ويختفي عنصر الحالة بالكامل عندما لا يحتوي رسالة حتى لا يحجز مساحة فارغة في نهاية الصفحة.

## تحديث محفوظ قبل الإطلاق: محول الصيغ الصوتية

يتبع محول الصيغ الصوتية الآن التسلسل الصحيح: قبل اختيار ملف تظهر بطاقة الرفع فقط، ولا تظهر قائمة الصيغة أو زر التحويل المعطل. بعد رفع ملف تظهر بياناته، ثم اختيار الصيغة وزر التحويل. عند إزالة الملف تعود الشاشة إلى حالة الرفع النظيفة.

تستخدم قائمة الصيغة خيارات واضحة مع وصف وظيفي قصير لكل من MP3 وWAV وOGG/WebM وZIP. اختبرت الحالة الأولية وحالة ملف WAV عينة ثم فتح القائمة وإزالة الملف على قياس الهاتف؛ بقيت القائمة متصلة بالحقل وفي العرض المرئي، ونجح فحص TypeScript وبناء الإنتاج.

## تحديث محفوظ قبل الإطلاق: محرر الصور والمنزلقات

أصبح محرر الصور يعيد حساب معاينة الصورة عند تغيّر مساحة اللوحة، بما في ذلك عند ظهور لوحة منزلق أو تبديل تبويب الأدوات؛ لذلك لا يتمدد الـ canvas ولا تتداخل أشرطة التحكم عند تعديل صورة على الهاتف. ثُبّت اتجاه المنزلقات الرقمية على **منخفض يساراً ومرتفع يميناً** رغم اتجاه النص العربي، وأصبح هذا السلوك مشتركاً في جميع الأدوات.

أزيل العرض البصري لرسائل الحالة السفلية من جميع الأدوات؛ لا تظهر الآن رسالة نجاح أو تحميل أو فراغ في نهاية الصفحة. يحتفظ المنطق بالنص داخلياً لأغراض التشخيص من دون عرضه للمستخدم. اختبر محرر الصور بعينة فعلية على قياس الهاتف: الضغط قرب 78% من يمين منزلق السطوع أعطى القيمة الموجبة 59، وبقي عرض الصفحة 393px بالكامل من دون تجاوز، وبقي عنصر الحالة مخفياً بارتفاع صفر.

## تحديث محفوظ قبل الإطلاق: قبول مزيل الخلفية

تم التحقق من مزيل الخلفية بعد تطبيق إخفاء رسائل الحالة العام. بعد إدخال صورة عينة وظهور زر «تحميل الصورة (PNG شفاف)»، بقي عنصر الحالة مخفياً وبارتفاع صفر، ولم يظهر النص الأخضر أسفل الأداة. على قياس الهاتف 393×852 كان عرض المستند 393px مطابقاً للنافذة، لذلك لا توجد مساحة رسالة أو تجاوز أفقي. الأداة مقبولة للانتقال إلى التالية.

## تحديث محفوظ قبل الإطلاق: ضاغط الصور

أزيل عنصر إدخال المجلد الأصلي الذي كان يظهر كزر نظام ثانٍ («اختيار الملفات / لم يتم اختيار أي ملف») أسفل زر «اختر مجلداً كاملاً». بقي الزر المصمم هو التفاعل الوحيد؛ يفتح اختيار المجلد نفسه، لكن الحقل النظامي أصبح مخفياً تماماً عن الواجهة.

اختبرت ضغط ثلاث صور على قياس الهاتف 393×852. ظهرت خلاصة الضغط وزر تنزيل ZIP، وبقيت رسالة «اكتمل ضغط…» مخفية بارتفاع صفر، كما بقي عرض الصفحة 393px. نجح فحص TypeScript وبناء الإنتاج.

## تحديث محفوظ قبل الإطلاق: قارئ PDF

أصبحت منطقة رفع ملف قارئ PDF ذات حد **متصل 1px** بدلاً من المتقطع. بعد فتح مستند يظهر في شريط القارئ زر كامل العرض «فتح ملف آخر» يفتح حقل الـPDF نفسه لاختيار مستند جديد من دون العودة إلى الصفحة الأولى أو فقدان عناصر التحكم.

اختبرت فتح مستند PDF فعلي من صفحتين على قياس الهاتف 393×852. ظهر الزر الجديد بعرض 343px داخل القارئ، وكان حد منطقة الإسقاط `solid` بسماكة 1px، وبقيت رسالة الحالة السفلية مخفية بارتفاع صفر وعرض الصفحة 393px. حُذف ملف الاختبار المؤقت من المصدر، ونجح فحص TypeScript وبناء الإنتاج.

## تحديث محفوظ قبل الإطلاق: ضاغط PDF

أصبحت منطقة رفع ضاغط PDF ذات حد **متصل بسماكة 1px** في الحالتين قبل وبعد اختيار الملف. بعد اختيار ملف PDF صالح لا تظهر رسالة «تم اختيار…» أسفل زر الضغط؛ تحفظ داخلياً فقط ولا تترك فراغاً مرئياً.

اختبرت اختيار ملف PDF على قياس الهاتف 393×852. بقيت الحافة `solid 1px`، وفُعّل زر بدء الضغط، وكان عنصر الحالة العام مخفياً بارتفاع صفر وعرض الصفحة 393px. نجح فحص TypeScript وبناء الإنتاج.

## تحديث محفوظ قبل الإطلاق: محرر PDF الشامل

أصبحت منطقة رفع ملفات محرر PDF الشامل ذات حد **متصل بسماكة 1px**. بعد تحميل PDF لا تظهر رسالة «تم تحميل…» السفلية؛ تحفظ داخلياً فقط ولا تترك مساحة في الذيل.

اختبرت تحميل ملف PDF من صفحتين على قياس الهاتف 393×852. حمّل المحرر الصفحتين بنجاح، وبقيت الحافة `solid 1px` وعنصر الحالة العام مخفياً بارتفاع صفر، ولم يتجاوز العرض 393px. حُذف ملف الاختبار المؤقت من المصدر، ونجح فحص TypeScript وبناء الإنتاج.

## تحديث محفوظ قبل الإطلاق: تشفير النصوص

أصبحت مساحة كتابة النص المشفّر أو المراد تشفيره سطحاً صلباً غير ضبابي، بلون داكن ثابت في الوضع الداكن وأبيض ثابت في الوضع الفاتح. واستُبدلت أيقونة الترس في زر توليد المفتاح بأيقونة تبديل عشوائي؛ فهي تعبّر عن إنشاء مفتاح جديد بدلاً من الإعدادات.

اختبرت الأداة على قياس الهاتف 393×852. مساحة النص والبطاقة كانتا بخلفية `rgb(10, 11, 14)` وبلا تمويه، وزر التوليد أنشأ مفتاحاً من 64 رمزاً، وبقيت رسالة الحالة المخزنة مخفية بارتفاع صفر ولم يتجاوز العرض 393px. نجح فحص TypeScript وبناء الإنتاج.

## تحديث محفوظ قبل الإطلاق: مصفاة النصوص

استعيدت بنية القوائم المدمجة في مصفاة النصوص كما في المرجع: قوائم استخراج البيانات والترميز أحادية الاختيار، وقائمة تنظيف متعددة الاختيار تبقى مفتوحة عند تحديد أكثر من فلتر. القوائم تتصل بالحقل أعلاه أو أسفله بحسب المساحة المتاحة، وتستخدم سطحاً صلباً لا تمويه فيه.

أصبحت مساحتا الإدخال والمخرجات صلبتين بلا تمويه. كما استبدلت رموز الواجهة غير المطابقة بأيقونة قمع للرأس، وأيقونة توسعة، وأيقونة سلة للحذف، وقفل للترميز. في اختبار 393×852 التصقت القوائم الثلاث بحقولها، واستخراج الرابط والتصفية المتعددة عملا بصورة صحيحة، وبقيت حقول النص بخلفية `rgb(10, 11, 14)` وبلا تمويه ولا تجاوز أفقي. نجح فحص TypeScript وبناء الإنتاج.

## تحديث محفوظ قبل الإطلاق: مقارنة النصوص

أزيلت استدعاءات رسائل النجاح من أداة مقارنة النصوص نفسها، ولذلك لا تظهر بعد الآن عبارة «اكتملت المقارنة…» أسفل زر نسخ النتيجة. بقيت بطاقة الفروق والإحصاءات وزر النسخ كما هي، فيما تبقى رسائل الخطأ فقط قابلة للتشخيص داخلياً.

اختبرت مقارنة نصين مختلفين؛ ظهرت الأسطر المضافة والمحذوفة وإحصاءاتها، ولم يُكتب أي نص نجاح في عنصر الحالة الذي ظل مخفياً بارتفاع صفر. نجح فحص TypeScript وبناء الإنتاج.

## تحديث محفوظ قبل الإطلاق: QR

أزيلت استدعاءات نجاح التوليد وقراءة الصورة ونسخ البيانات من أداة QR نفسها، ولذلك لا تظهر عبارات مثل «تم إنشاء رمز QR محلياً» أو «تمت قراءة رمز QR بنجاح» في نهاية الصفحة. بقيت بطاقة بيانات الرمز، زر النسخ، التوليد، وتنزيل PNG دون تغيير، فيما تبقى الأخطاء قابلة للتشخيص داخلياً.

اختبرت إنشاء رمز لرابط فعلي؛ ظهرت المعاينة وزر تنزيل PNG، وبقي عنصر الحالة مخفياً بارتفاع صفر ومن دون نص نجاح. نجح فحص TypeScript وبناء الإنتاج.

## ملفات أدلة الاختبار

أُرفقت في مجلد العمل الذي أُنتجت منه الحزمة، وليست مطلوبة للتشغيل: `mobile_repair_investigation.md` وسجل مسارات HTTP ولقطات القبول. تقرير التشغيل هذا وحده مضمّن في جذر المصدر كي يبقى المشروع خفيفاً.

## تحديث محفوظ قبل الإطلاق: الأرشفة والتشفير

راجعت تطبيق الأرشفة الحديث مقابل المرجع المحلي. تحولت بطاقات الأداة ومنطقة الإسقاط وحقول الإدخال والقائمة إلى **أسطح صلبة** في السمة الداكنة، وألغيت مرشحات التمويه المحلية. أصبحت حافة منطقة السحب والإفلات `solid 1px` بدلاً من المتقطعة، مع إبقاء لون السحب النشط واضحاً.

فُصل زر «اختر مجلداً كاملاً» عن منطقة اختيار الملفات التي كانت `label` متداخلة. يستدعي الزر الآن حقل `webkitdirectory`/`directory` وحده، بينما تفتح مساحة الإسقاط منتقي الملفات. أضيفت أوصاف عملية لمستويات الضغط، وأصبحت القائمة المخصصة ذات سطح صلب ومتّصلة بالحقل وتفتح لأعلى أو لأسفل وفق المساحة المتاحة. كما استبدلت أيقونة الترس في زر توليد المفتاح بأيقونة مفتاح دلالية موافقة للمرجع.

في محاكاة هاتف **393×852**، بقي `scrollWidth` مساوياً لـ`clientWidth`، وكانت منطقة الإسقاط بحد متصل، والقائمة بعرض الحقل نفسه وضمن الشاشة. أثبت اختبار زر المجلد أنه يستدعي `ae-folder` وحده مع السمتين المطلوبتين؛ كما حُدّث عدّاد الأداة بنجاح لملف داخل مسار مجلد نسبي. اختبر تغيير مستوى الضغط ووصلت القيمة إلى select الأصلي. نجح كذلك `pnpm check` و`pnpm build`.

## تحديث محفوظ قبل الإطلاق: الإعدادات والصفحات القانونية

استُبدل رمز زر تسجيل الدخول بعلامة Google الرسمية متعددة الألوان بدلاً من الحرف المبسط، كما استبدل رمز الانتقال إلى عضوية VIP بألماسة ذهبية. حُسنت كذلك الرموز الدلالية لروابط السياسة والخصوصية وشروط الاستخدام لتطابق المرجع؛ فأصبحت الدرع للخصوصية والوثيقة المحررة للشروط.

أعيد بناء صفحتي **سياسة الخصوصية** و**شروط الاستخدام** داخل صفحة الإعدادات الحديثة بدلاً من الملخصين السابقين. تحتوي صفحة الخصوصية أربعة أقسام وجدولاً من خمس خانات يوضح التعامل مع الملفات والحساب والتفضيلات والفيدباك والإعلانات، إضافة إلى تنبيه خصوصية ختامي. تحتوي صفحة الشروط ستة أقسام كاملة عن القبول والاستخدام المقبول والمحظور والمدفوعات والملكية وإخلاء المسؤولية، مع رابط التواصل عبر الفيدباك.

في محاكاة هاتف **393×852**: لم يحدث تجاوز أفقي في الإعدادات أو أي من الصفحتين. ظهر رمز Google بألوانه الأربع، وظهر رمز الألماسة بلون `rgb(245, 166, 35)`. تم التحقق من ظهور أربعة أقسام وخمسة صفوف جدول في الخصوصية، وستة أقسام و12 بنداً في الشروط. نجح أيضاً `pnpm check` و`pnpm build`.

## تحديث محفوظ قبل الإطلاق: لوحة التنقل والحركة

قورنت لوحة التنقل الحديثة بقواعد المرجع في المستودع. أصبحت اللوحة تستخدم ظلّاً جانبياً ثابتاً مع توهج أزرق خافت في أعلى اللوحة، وتتبع حركة دخول/خروج مدتها 300ms بمنحنى مريح. أصبحت الخلفية المغطية تتلاشى خلال 260ms، وأعيدت الحواف المستديرة المدمجة وحالات الصفوف النشطة مع خط داخلي متوهج بحسب لون الفئة.

تغير سلوك الأقسام من إظهار فوري يعتمد على الخاصية `hidden` إلى طي وتمدد فعليين: تتغير `max-height` والحشوة والشفافية والإزاحة الرأسية معاً. يبقى القسم المغلق غير قابل للتركيز باستخدام `inert` و`aria-hidden`، وتتحرك علامة السهم بسلاسة. أضيفت أيضاً استجابة لمس قصيرة `scale(.975)` للأزرار والصفوف، مع احترام تفضيل تقليل الحركة في النظام.

في محاكاة هاتف **393×852**، فتحت اللوحة بعرض 280px ضمن الشاشة، ووصلت طبقة التعتيم إلى الشفافية الكاملة بعد الحركة. اختبر الانتقال من فئة الفيديو إلى الصوت: ظهرت في منتصف الحركة قيمة ارتفاع تدريجية وشفافية متوسطة لكلا القسمين، ثم استقر القسم المفتوح بقيمة `max-height: 380px` والقسم المغلق عند `0px`. حافظت الصفحة على `scrollWidth = clientWidth = 393`، ونجح `pnpm check` و`pnpm build`.

## تحديث محفوظ قبل الإطلاق: دليل الأدوات

أعيد تنسيق دليل الأدوات ليصبح نظام بطاقات مستقلة بدلاً من بطاقات متقاربة بصرياً. أصبحت البطاقة ذات سطح داكن صلب، حافة مستديرة أكبر، شريط فئة جانبي واضح، وتوهج لوني خافت مقيد داخل كل بطاقة. زادت المسافة الرأسية بين البطاقات على الهاتف إلى **17px**، مع فصل ثابت وواضح في كامل القائمة.

نُظم محتوى البطاقة داخل منطقة مستقلة: رمز SVG الدلالي الخاص بالأداة، ووسم الفئة، والعنوان والوصف، ووسما المزايا، وتعليمات الاستخدام السريع، ثم تذييل موحد يحوي رمز سهم SVG بدل السهم النصي. استُخدمت الرموز المعرفة لكل أداة فعلياً، لذلك لا تتكرر أيقونة عامة حيث يوجد رمز متخصص للضغط أو الصوت أو القص أو PDF أو النصوص أو المشاركة.

في محاكاة هاتف **393×852** ظهرت 18 بطاقة، وكلها تحتوي رمزاً وعنواناً ووصفاً وتفاصيل وتذييل رابط. كان الحد الأدنى والأقصى للفاصل بين البطاقات **17px**، وخلفية البطاقة `rgb(10, 13, 18)` الصلبة وحافتها `14px`، ولم يحدث تجاوز أفقي (`scrollWidth = 393`). نجح `pnpm check` و`pnpm build`.

## تحديث محفوظ قبل الإطلاق: تسلسل عناوين الرئيسية

صُحح التسلسل الدلالي في بطل الصفحة الرئيسية من دون أي تغيير في الأصناف البصرية أو تدرج النص. أصبح عنوان **«الترسانة»** هو `H1` الرئيسي للصفحة، وأصبحت جملة **«كل أدواتك في مكان واحد»** العنوان `H3` التوضيحي، مع استمرار تأثير النص المتدرج نفسه.

تحقق DOM المحلي من وجود `H1` بالنص «الترسانة» و`H3` بالنص «كل أدواتك في مكان واحد». نجح أيضاً `pnpm check` و`pnpm build`.

## تحديث محفوظ قبل الإطلاق: استبدال محرر الفيديو بمنشئ GIF

استُبدل محتوى المسار `/tools/video-editor/` بالكامل بأداة **منشئ GIF**، مع بقاء المسار نفسه حفاظاً على الروابط القديمة. تغير الاسم والوصف والرمز في تعريف الأدوات، ودليل الأدوات والتنقل والبحث وعنوان المتصفح والبيانات المنظمة، إلى: «منشئ GIF — اقتطاع وتحويل الفيديوهات إلى GIF فورياً من المتصفح».

تعتمد الأداة محلياً على عنصر الفيديو وCanvas، ثم على مكتبة `gif.js` المرخصة MIT وعاملها المحزم مع Vite. لا تستخدم FFmpeg/WASM ولا ترفع الفيديو إلى خادم. تشمل منطقة رفع بحد `solid 1px`، ومعاينة تشغيلية، ونطاقي بداية ونهاية للمقطع مع سقف موصى به 15 ثانية، وFPS من 10 إلى 60، وأبعاداً مع قفل نسبة المصدر ومقاسات سريعة. تحسب الحاسبة الإطارات تلقائياً وفق `المدة × FPS` وتعرض تقدير حجم ودقة، وتحذر عند تجاوز 50 FPS أو 400 إطار، ويضبط زر التحسين FPS إلى 22 ومقاساً متوازناً.

اختبر السيناريو الحقيقي على 393×852 بملف `editor-test.mp4` حجمه 28.5KB ومدته ثانيتان ودقته 320×180. ظهرت مساحة العمل والحاسبة (`40` إطاراً افتراضياً) بلا تجاوز أفقي وباتجاه نطاق LTR. ظهر تحذير FPS عند 60، ثم أزاله التحسين بضبط 22 FPS. صُدّر مقطع 0.8 ثانية عند 10 FPS و214×120 عبر العامل الخلفي إلى GIF حجمه 11.9KB، بثمانية إطارات وترويسة فعلية `GIF89a`، وظهر رابط تنزيل باسم `Arsenal_editor-test.gif`. نجح `pnpm check` و`pnpm build`، وتضمن بناء الإنتاج العامل المحلي `gif.worker-*.js`.

## تحديث محفوظ قبل الإطلاق: قائمة اللغة المنسدلة في الإعدادات

استُبدلت شبكة بطاقات اللغة بالكامل بقائمة Arsenal منسدلة مخصصة داخل بطاقة لغة صلبة. تعرض البطاقة عنواناً ووصفاً موجزاً وحقل اختيار بعرض كامل، مع قائمة متصلة بالحقل وحركة فتح وإغلاق قصيرة؛ ولا تعتمد نافذة Android/Chrome الأصلية. أُضيف تحميل أنماط `arsenal-select.css` داخل صفحة الإعدادات، وأُلغي إرث شبكة العمودين وحشوة الحقول القديمة الذي كان يضيّق الحقل ويترك فجوة بينه وبين القائمة.

تضم القائمة النهائية تسع لغات: العربية، English، Español، Français، Deutsch، Türkçe، Русский، हिन्दी، 中文. عند الاختيار تتحدث القيمة المرئية والحقل الأصلي المخفي، ويُحفظ رمز اللغة في `localStorage` بالمفتاح `arsenal_lang`، ثم يعاد رسم الإعدادات بالقيمة المحفوظة. لم يُضف أي تنبيه سفلي مرئي.

اختبار 393×852 أكد وجود الخيارات التسعة بالترتيب المحدد، وعرض زر وقائمة مقداره 343px لكل منهما، مع التصاق عمودي بمقدار 1px (القائمة تبدأ قبل نهاية الحقل ببكسل واحد)، واحتواء كامل داخل عرض الشاشة من 25px إلى 368px، و`scrollWidth=393`. اختيرت Español في الاختبار فتغير الحقل إليها وبقيت القيمة `es` في الحقل المخفي وفي `localStorage` مع بقاء حالة الشريط السفلي مخفية. نجح `pnpm check && pnpm build` بعد التعديل.


## تحديث محفوظ قبل الإطلاق: دعم اللغات التسع لجميع الصفحات والأدوات

أضيفت طبقة ترجمة ثابتة ومكتوبة النوع داخل `src/shared/i18n/` تدعم العربية (`ar`) والإنجليزية (`en`) والإسبانية (`es`) والفرنسية (`fr`) والألمانية (`de`) والتركية (`tr`) والروسية (`ru`) والهندية (`hi`) والصينية (`zh`). تحفظ `setLocale()` رمز اللغة في `arsenal_lang`، وتضبط `document.documentElement.lang` و`dir` و`data-locale` عند بدء كل صفحة. العربية وحدها تستخدم `rtl`، بينما تستخدم اللغات الثماني الأخرى `ltr`.

تغطي الطبقة الغلاف المشترك والتنقل والبحث وبيانات الأدوات وعناوين المتصفح والصفحة الرئيسية ودليل الأدوات والإعدادات والصفحات القانونية، إضافة إلى الأدوات الثماني عشرة: منشئ GIF، ضغط الفيديو، فيديو إلى صوت، محول الصوت، مزيل الضوضاء، سرعة الصوت، محرر الصور، مزيل الخلفية، ضاغط الصور، إنشاء PDF، ضاغط PDF، محرر PDF، تشفير النصوص، مصفاة النصوص، مقارنة النصوص، مولد وقارئ QR، الأرشفة والتشفير، والمشاركة المحلية. استُخدمت قواميس مراجَعة للنصوص البارزة ومفردات التفاعل الحساسة، مع قواميس ثابتة احتياطية للنصوص الأقل ظهوراً؛ لا توجد خدمة ترجمة أو طلب شبكة وقت تشغيل المستخدم.

يحافظ مترجم DOM المحدود على محتوى المستخدم: يتجاهل حقول `input` و`textarea` و`select` و`option` و`pre` و`code` والعناصر القابلة للتحرير، فلا يغيّر النص الذي يدخله المستخدم أو مخرجات المعالجة. وتبقى الامتدادات وأسماء الصيغ والبروتوكولات والخوارزميات والقيم البرمجية كما هي. حُوّلت رسائل تقدم Worker في مزيل الضوضاء إلى بيانات محايدة ثم تُعرض بلغتها من الواجهة، فيما بقيت رسائل الحالة السفلية العامة مخفية حسب القرار السابق.

| تحقق اللغة والواجهة على الهاتف 393×852 | النتيجة |
|---|---|
| 21 مساراً مستقلاً × 9 لغات | **189/189** حالة فُحصت |
| `documentElement.lang` و`dir` | مطابقان للغة المختارة في جميع الحالات |
| تجاوز عرض الصفحة | لا يوجد؛ `scrollWidth ≤ clientWidth` في الحالات جميعها |
| نص عربي غير مقصود في واجهات LTR | لا يوجد؛ استُثني فقط اسم اللغة الأصلي «العربية» ووصفه داخل قائمة اللغات |
| فحص TypeScript | `pnpm check` ناجح |
| بناء Vite للإنتاج | `pnpm build` ناجح |
| منشئ GIF | اختبار رفع وتحسين وتصدير GIF89a حقيقي ناجح باللغة الصينية مع بقاء المنزلقات LTR |
| مولد QR | اختبار إدخال رابط وتوليد رمز QR فعلي وإتاحة تنزيل PNG ناجح باللغة الصينية |

> لا تزال أسماء اللغة الأصلية داخل قائمة الاختيار مقصودة لتسهيل التعرف عليها، مثل «العربية»، و`English`، و`Español`، و`中文`. لا تمثل هذه الأسماء بقايا ترجمة عربية في واجهات LTR.


## تحديث محفوظ قبل الإطلاق: PWA والعمل دون اتصال

أضيف دعم PWA فعلي إلى النسخة متعددة الصفحات. يولّد Vite بيان `manifest.webmanifest` موحداً، وأيقونتي Arsenal بالمقاسين **192×192** و**512×512**، ويحقن رابط البيان ولون الواجهة وإعدادات Apple في المداخل الـ21 جميعها. يسجل الغلاف المشترك `Service Worker` على النطاق الجذري `/` في السياقات الآمنة فقط، فلا يحاول التسجيل في متصفح لا يدعم الخدمة ولا يفرض إعادة تحميل قد تقطع معالجة ملف أو إدخال المستخدم.

استُبدل منطق زر **«تحميل للاستخدام Offline»** في الإعدادات، الذي كان يحفظ HTML للمسارات فقط، بطلب مباشر إلى العامل. عند الضغط، يخزّن العامل مسارات الرئيسية والدليل والإعدادات والأدوات الثماني عشرة، مع ملفات JavaScript وCSS وWorkers والأصول المحلية الفعلية التي ولّدها البناء. يعرض الزر تقدماً حقيقياً داخل بطاقة الإعدادات ثم حالة نجاح مترجمة، ويظل قابلاً للضغط مجدداً لتحديث النسخة المحلية. أضيفت الحالات الجديدة باللغات التسع، ولا تستخدم رسالة الحالة السفلية المخفية عالمياً.

| عنصر PWA | التنفيذ والتحقق |
|---|---|
| بيان التثبيت | رابط `manifest.webmanifest` واحد في **21/21** صفحة إنتاج، ولا تبقى إشارة إلى `manifest.json` قديم. |
| نطاق العامل | العامل في `/service-worker.js` وبنطاق `/`؛ ينظف كاش إصدار البناء السابق عند التفعيل ويحتفظ بكاش وقت الاستخدام. |
| التخزين المسبق | في اختبار بناء نظيف حُفظت **104** موارد، ومنها الجذر والإعدادات والدليل ومسارا QR وGIF وكل أصول البناء اللازمة لواجهات الأدوات. |
| استراتيجية الطلب | صفحات التنقل تستخدم الشبكة أولاً ثم النسخة المخزنة عند الانقطاع؛ أصول التطبيق نفسها تستخدم الكاش أولاً ثم تحفظ عند أول طلب ناجح. |
| الموارد الثقيلة | لا ينزّل زر الصفحات نماذج ONNX أو محرك ONNX المحلي الكبير تلقائياً. موارد FFmpeg/ONNX البعيدة تُخزّن عند أول استخدام ناجح، لذلك يمكن للأدوات المعتمدة على نموذج أو محرك لم يُنزّل سابقاً أن تتطلب اتصالاً في استخدامها الأول. |

أجري اختبار مستقل في Chromium بملف تعريف جديد وعلى **393×852**. في البداية سُجّل العامل ضمن `localhost`، ضغط زر التنزيل حتى **100%**، وظهر كاش أساس واحد باسمه الإصدارّي. بعد محاكاة انقطاع كامل للشبكة، فُتحت الصفحة الرئيسية بالعربية RTL، ودليل الأدوات وQR بالإنجليزية LTR، والإعدادات ومنشئ GIF بالصينية LTR، ومزيل الضوضاء بالعربية RTL. ظهرت في كل حالة طبقة الغلاف ومساحة العمل وملفات CSS، وكانت `scrollWidth ≤ clientWidth` من دون تجاوز أفقي.

استكمل اختبار الوظائف المحلية دون اتصال لمولد QR ومنشئ GIF. ولّد QR رمزاً حقيقياً من نص الاختبار وأظهر تنزيل PNG وهو غير متصل. كما رُفع فيديو العينة إلى منشئ GIF، وظهر تحذير 60 FPS، وضبط زر التحسين القيمة إلى 22 FPS، وصُدّر GIF حقيقي بترويسة `GIF89a` وحجم 11.9KB. نجح `pnpm check` و`pnpm build` بعد الإضافة؛ يبقى تحذير Vite الوحيد المعروف متعلقاً بحجم chunk `tool-shell` فوق 500KB وليس خطأ بناء.

> يتطلب التثبيت والعمل دون اتصال سياقاً آمناً: HTTPS في النشر، أو `localhost`/`127.0.0.1` على Termux. عند فتح الموقع من عنوان IP محلي عبر HTTP غير آمن قد يمنع Chrome تسجيل Service Worker؛ لذلك يستخدم الهاتف عنوان localhost المعروض من Vite.


## تحديث محفوظ قبل الإطلاق: SEO تقني دقيق

أضيفت طبقة SEO مركزية في `src/shared/seo.ts` تصف كل مسار منشور باسم فريد وعنوان ووصف طبيعيين وحالة فهرسة ونوع Schema مناسب. يعتمدها بناء Vite مباشرةً، لذلك تُولد البيانات الوصفية من مصدر واحد للمسارات الـ21 بدلاً من تكرار 18 كتلة HTML يدوياً. صارت كل صفحة مفهرسة تملك `title` و`meta description` و`robots` وcanonical وحقول Open Graph وTwitter وصورة مشاركة مطلقة، بينما تبقى صفحة الإعدادات `noindex, follow` لأنها واجهة تفضيلات وليست صفحة اكتساب بحث.

تتبع العناوين والأوصاف قاعدة أن تكون موجزة ووصفية ومميزة لكل أداة، لا قائمة كلمات مفتاحية مكررة؛ وهو متسق مع إرشادات Google للعناوين والأوصاف [1] [2]. تستخدم الرئيسية `WebSite` و`WebApplication` و`WebPage`، ويستخدم دليل الأدوات `CollectionPage` مع `ItemList` للأدوات الثماني عشرة، فيما تستخدم كل أداة `SoftwareApplication` و`WebPage` يصفان وظيفتها الحقيقية. اختير JSON-LD لأنه صيغة موصى بها وسهلة الصيانة، ولم تُضف تقييمات أو أسعار أو ادعاءات أو خصائص لا تظهر أو لا تنطبق على المنصة [3].

| مجال الفهرسة | النتيجة |
|---|---|
| صفحات الإنتاج ذات title وdescription وcanonical وrobots | **21/21** |
| صفحات الإنتاج ذات Open Graph وTwitter وJSON-LD المناسب | **21/21**؛ الإعدادات تخلو من Schema عمداً لأنها `noindex` |
| Canonical | رابط HTTPS واحد صحيح لكل مسار، متسق مع المسار داخل `sitemap.xml` |
| خريطة الموقع | **20** صفحة مفهرسة: الرئيسية والدليل و18 أداة؛ الإعدادات مستثناة قصداً |
| المحتوى دون JavaScript | عنوان ووصف ونص توضيحي ورابط إلى الدليل ظاهرون داخل `noscript` لكل مسار |
| PWA بعد SEO | اختبار جديد بملف تعريف نظيف نجح؛ تنزيل 104 موارد والعمل دون اتصال بقي سليمين |

تولَّد `sitemap.xml` أثناء البناء من سجل SEO نفسه مع `lastmod` موحد بتاريخ `2026-08-27`، وتبقى إشارة الخريطة في `robots.txt`. أزيلت إشارات المسارات القديمة من الخريطة الحديثة؛ لذلك يجب نشر هذه النسخة كي تستبدل خريطة الموقع الموجودة حالياً في النطاق، والتي ما زالت تسرد بعض الأسماء القديمة. يظل canonical إشارة تفضيلية لا ضماناً، وتدعم خريطة الموقع والإحالات الداخلية اختيار الرابط الممثل [4].

لا تستخدم المنصة عناوين مستقلة للغات التسع؛ اللغة تتغير محلياً بـ`localStorage` في URL نفسه. لهذا لم يُضف `hreflang` مصطنع، إذ إن تنفيذه الصحيح يتطلب URLs منفصلة ومحتوى يمكن لمحركات البحث الوصول إليه. لا توجد بيانات Google Search Console أو تحليلات زيارات أو كلمات مفتاحية في مساحة العمل، لذلك لا يدعي هذا التحديث ارتفاع ترتيب أو زيارات؛ بعد النشر يمكن إرسال `https://arsenal-atl.pages.dev/sitemap.xml` إلى Search Console وطلب إعادة الزحف للصفحات المهمة وقياس النتيجة هناك.

### مراجع SEO

[1]: https://developers.google.com/search/docs/appearance/title-link "Google Search Central: Title links"
[2]: https://developers.google.com/search/docs/appearance/snippet "Google Search Central: Snippets and meta descriptions"
[3]: https://developers.google.com/search/docs/appearance/structured-data/intro-structured-data "Google Search Central: Structured data"
[4]: https://developers.google.com/search/docs/crawling-indexing/canonicalization "Google Search Central: Canonicalization"

## تحديث محفوظ قبل الإطلاق: استعادة اشتراك VIP والدفع التلقائي

استعيدت واجهة عضوية VIP ضمن صفحة الإعدادات الحديثة من دون إعادة أي بنية قديمة أو مفاتيح سرية إلى المتصفح. تعرض الصفحة ثلاث خطط واضحة: مجانية، وVIP شهري بقيمة **5 USD**، وVIP سنوي بقيمة **50 USD** مع وفورات مقارنة بالسداد الشهري. لا يمكن للحساب غير المسجل إنشاء أي طلب دفع؛ تظهر له رسالة واضحة وزرا الخطتين المدفوعتين معطلين، ويبدأ التدفق فقط بعد ربط هوية Google ذات المعرّف `sub` بالحساب المحلي.

عند اختيار خطة لحساب مسجل، تفتح أولاً نافذة تأكيد لا تنشئ أي طلب. لا يستدعى مسار إنشاء الدفع إلا عند الضغط الصريح على زر «إنشاء عنوان الدفع». بعد الاستجابة الصحيحة، تظهر داخل النافذة فقط بيانات USDT BEP20 وعنوان قابل للنسخ والمبلغ المطلوب وتحذير شبكة BSC، ثم تجري مراقبة التفعيل فوراً وكل 10 ثوانٍ لمدة تصل إلى 6 دقائق. لا تُحدَّث مفاتيح الاشتراك المحلية إلا إذا أعاد العامل حالة نشطة مع خطة شهرية أو سنوية صالحة، وتُمسح الحالة المحلية المتقادمة إذا أعاد العامل أن الاشتراك غير نشط. لا توجد رسالة حالة عامة في أسفل الصفحة.

| مجال التحقق | النتيجة الصادقة |
|---|---|
| عامل حالة الاشتراك في الإنتاج | طلب GET للقراءة فقط إلى `check-subscription` بمعرّف صحي أعاد **200** وسمح بأصل `https://arsenal-atl.pages.dev`. لم ينشئ هذا الطلب فاتورة أو معاملة. |
| إنشاء الدفع والمراقبة | اختُبرا بردود `fetch` محاكاة محلية فقط: عنوان ومبلغ، نسخ العنوان، حالة غير نشطة ثم تفعيل شهري، وتحديث البطاقة بعد الإغلاق. |
| مسار الخطأ | استجابة خطأ محاكاة لا تعرض أي وصف داخلي للعامل؛ تظهر رسالة مترجمة عامة ويظل زر إعادة المحاولة متاحاً. |
| اللغات | فُحصت العربية والإنجليزية والصينية؛ ظهرت بطاقات الخطط والتنبيهات والوفورات مترجمة، والعربية RTL والإنجليزية/الصينية LTR. |
| الهاتف | عند **393×852** كان `scrollWidth = clientWidth = 393`، وظهرت البطاقات بعمود واحد مع تعطيل الدفع للحساب غير المسجل. |
| الجودة | `pnpm check` و`pnpm build` نجحا؛ يبقى تحذير Vite المعروف الخاص بحجم `tool-shell` فقط. |

> لم يُنشأ طلب دفع حقيقي أو فاتورة NowPayments ولم تُنفذ معاملة خلال هذا التدقيق. يظل الاختبار المالي الحقيقي خارج النطاق إلى أن يقدّم مالك المنصة تأكيداً فورياً وصريحاً يتحمل أثر إنشاء طلب الدفع.
