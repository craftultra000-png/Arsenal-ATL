# التحقق المرئي — المعاينة المحلية

تم فتح الصفحة الرئيسية على `http://127.0.0.1:4175/` وتحققت روابط الأدوات المعروضة مباشرة إلى `/tools/<slug>/`. ظهرت الهوية الداكنة والشعار والبطاقات والبحث والفلاتر بصورة سليمة ضمن لقطة المعاينة.

تم فتح `http://127.0.0.1:4175/tools/image-editor/` مباشرةً. ظهر الغلاف المستقل ومساحة العمل وعناصر الإدخال بنجاح، ما يؤكد استقلال المسار عن أي صفحة تشغيل مركزية. لكن لقطة المعاينة أظهرت تراكباً واضحاً بين حقول إعدادات محرر الصور داخل شبكة النموذج؛ يجب إضافة تنسيق مخصص لحقول `range` و`output` والوسوم في المحرر قبل اعتباره جاهزاً بصرياً.


بعد تصحيح فئة شبكة النماذج، أُعيد فتح محرر الصور مباشرة. اختفى التراكب الحاد بين مجموعات الحقول، وأصبح كل تحكم ضمن مسار الشبكة. لا تزال عناصر المدى واسعة بصرياً في لقطة سطح المكتب، ولذلك يمكن تحسين التباعد والعناوين في جولة CSS لاحقة، لكنه ليس عيباً يمنع الاستخدام.

فُتحت صفحة إنشاء PDF مباشرةً وظهرت حقول العنوان واسم الملف والمحتوى وزر الإنشاء ضمن الغلاف المستقل. المسار والحمولة الخاصة بالأداة يعملان، لكن ترتيب منطقة النص في لقطة الواجهة يحتاج ضبطاً بصرياً إضافياً ضمن شبكة الحقول قبل التسليم النهائي.


أُجري اختبار وظيفي لمنشئ PDF عبر إدخال عنوان ونص عربيين تجريبيين ثم تشغيل زر الإنشاء. ظهرت رسالة النجاح `تم إنشاء ملف PDF وتنزيله.` في الواجهة، ما يؤكد نجاح التحميل الديناميكي لمكتبة PDF والتصدير المحلي في المعاينة.


فُتحت صفحة المشاركة المحلية مباشرةً على مسارها النهائي. ظهرت خطوات المرسل والمستلم، حقول تبادل العرض والرد، وإرشاد WebRTC. بعد إضافة الأنماط المخصصة أصبحت الحقول مرتبة ضمن قسمين واضحين، مع بقاء التخطيط قابلاً للعرض على مساحة سطح المكتب المعاينة. يلزم اختبار اتصال بجهازين فعليين ضمن فحص التكامل النهائي؛ لا يمكن محاكاة شبكة جهازين من لقطة واجهة واحدة.


أُجري اختبار وظيفي خفيف لمِصفاة النصوص على رابطها المستقل. أُدخل النص `Arsenal ATL` ثم استُخدم تحويل Base64، فظهر الناتج الصحيح `QXJzZW5hbCBBVEw=` ورسالة نجاح في الواجهة. يؤكد ذلك عمل التدفق المحلي من الإدخال إلى الناتج.
